/**
 * Stub classes for @better-auth/kysely-adapter dialect files.
 *
 * The kyseley adapter imports these from the removed `SqliteDialect` configs.
 * We use Drizzle + Postgres, so these are never instantiated.
 * Stubs prevent build errors from missing exports.
 */

export class BunSqliteDialect {}
export class NodeSqliteDialect {}
export class D1SqliteDialect {}
