# Text-to-SQL Agent Eval Report

**Provider**: deepseek　**Model**: deepseek-v4-flash　**Prompt**: v4　**Date**: 2026-09-09T07:52:45.573Z
**Commit**: 6982ee6725bd7605e53a58c31ff55429acb4d476　**Dataset**: retail-v1 (45ab11bdec5e)
**Source snapshot**: 75f0ac1e3a74　**Working tree dirty**: yes

## Overall

| Metric | Value |
|--------|-------|
| Tests | 50 |
| SQL validity | 100.0% |
| Replay execution success | 100.0% |
| SQL result accuracy | 74.0% |
| Schema adherence | 100.0% |
| Application tool success | 100.0% |
| Answer completeness | 100.0% |
| End-to-end task success | 74.0% |

## By category

| Category | Tests | SQL result accuracy | Task success |
|----------|-------|---------------------|--------------|
| simple | 8 | 100% | 100% |
| aggregation | 8 | 88% | 88% |
| join | 10 | 80% | 80% |
| time_series | 9 | 56% | 56% |
| multi_step | 10 | 40% | 40% |
| null_semantics | 2 | 100% | 100% |
| edge_case | 3 | 100% | 100% |

## Failed / incomplete cases

### join_04 — join
- **Q**: 下单金额最高的 10 位客户是谁？
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `SELECT c.first_name || ' ' || c.last_name AS customer_name, c.email, ROUND(SUM(o.total_amount), 2) AS total_order_amount
FROM orders o
JOIN customers c ON o.customer_id = c.customer_id
GROUP BY c.customer_id, c.first_name, c.last_name, c.email
ORDER BY total_order_amount DESC
LIMIT 10;`
- **rows**: gen=10 ref=10
- **application tool / answer / finish**: 1 / 1 / stop

### time_01 — time_series
- **Q**: Show the monthly sales trend for 2025.
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `SELECT TO_CHAR(order_date, 'YYYY-MM') AS month,
       SUM(total_amount) AS total_sales
FROM orders
WHERE order_date >= '2025-01-01'
  AND order_date < '2026-01-01'
GROUP BY TO_CHAR(order_date, 'YYYY-MM')
ORDER BY month;`
- **rows**: gen=12 ref=12
- **application tool / answer / finish**: 1 / 1 / stop

### time_02 — time_series
- **Q**: 2026 年每个季度的订单数量是多少？
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `SELECT EXTRACT(QUARTER FROM order_date) AS quarter, COUNT(*) AS order_count
FROM orders
WHERE order_date >= '2026-01-01' AND order_date < '2027-01-01'
GROUP BY EXTRACT(QUARTER FROM order_date)
ORDER BY quarter;`
- **rows**: gen=2 ref=2
- **application tool / answer / finish**: 1 / 1 / stop

### time_03 — time_series
- **Q**: How many new customers signed up each month in 2025?
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `SELECT DATE_TRUNC('month', signup_date)::date AS month,
       COUNT(*) AS new_customers
FROM customers
WHERE signup_date >= '2025-01-01'
  AND signup_date < '2026-01-01'
GROUP BY DATE_TRUNC('month', signup_date)
ORDER BY month;`
- **rows**: gen=12 ref=12
- **application tool / answer / finish**: 1 / 1 / stop

### multi_01 — multi_step
- **Q**: Which product category has the highest return rate?
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `WITH category_stats AS (
  SELECT p.category_id,
         SUM(CASE WHEN o.status = 'returned' THEN oi.quantity ELSE 0 END) AS returned_qty,
         SUM(oi.quantity) AS total_qty
  FROM order_items oi
  JOIN orders o ON oi.order_id = o.order_id
  JOIN products p ON oi.product_id = p.product_id
  GROUP BY p.category_id
)
SELECT c.category_name,
       co.returned_qty,
       co.total_qty,
       ROUND(co.returned_qty * 100.0 / co.total_qty, 2) AS return_rate_pct
FROM category_stats co
JOIN categories c ON co.category_id = c.category_id
ORDER BY return_rate_pct DESC
LIMIT 1;`
- **rows**: gen=1 ref=1
- **application tool / answer / finish**: 1 / 1 / stop

### multi_02 — multi_step
- **Q**: 哪个品牌的利润率最高？利润 = 售价 - 成本。
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `SELECT p.brand,
       SUM(oi.line_total) AS total_revenue,
       SUM(oi.quantity * p.cost) AS total_cost,
       (SUM(oi.line_total) - SUM(oi.quantity * p.cost)) / SUM(oi.line_total) AS profit_margin
FROM order_items oi
JOIN products p ON oi.product_id = p.product_id
GROUP BY p.brand
ORDER BY profit_margin DESC
LIMIT 1;`
- **rows**: gen=1 ref=1
- **application tool / answer / finish**: 1 / 1 / stop

### multi_03 — multi_step
- **Q**: What is the average number of items per order?
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `SELECT ROUND(SUM(quantity)::numeric / COUNT(DISTINCT order_id), 2) AS avg_items_per_order
FROM order_items;`
- **rows**: gen=1 ref=1
- **application tool / answer / finish**: 1 / 1 / stop

### multi_04 — multi_step
- **Q**: Platinum 等级客户贡献了多少比例的总销售额？
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `SELECT
  ROUND(100.0 * SUM(CASE WHEN c.loyalty_tier = 'Platinum' THEN o.total_amount ELSE 0 END)
             / NULLIF(SUM(o.total_amount), 0), 2) AS platinum_share_pct
FROM orders o
JOIN customers c ON o.customer_id = c.customer_id;`
- **rows**: gen=1 ref=1
- **application tool / answer / finish**: 1 / 1 / stop

### agg_05 — aggregation
- **Q**: What is the lowest product list price?
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `SELECT product_name, base_price
FROM products
ORDER BY base_price ASC
LIMIT 1;`
- **rows**: gen=1 ref=1
- **application tool / answer / finish**: 1 / 1 / stop

### join_10 — join
- **Q**: 退货订单中，每个产品共退回了多少件？只显示前 5 名。
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `SELECT p.product_name,
       SUM(oi.quantity) AS returned_quantity
FROM order_items oi
JOIN orders o ON oi.order_id = o.order_id
JOIN products p ON oi.product_id = p.product_id
WHERE o.status = 'returned'
GROUP BY p.product_id, p.product_name
ORDER BY returned_quantity DESC
LIMIT 5;`
- **rows**: gen=5 ref=5
- **application tool / answer / finish**: 1 / 1 / stop

### time_09 — time_series
- **Q**: Show average order value by month for 2025.
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `SELECT TO_CHAR(DATE_TRUNC('month', order_date), 'YYYY-MM') AS month,
       ROUND(AVG(total_amount), 2) AS avg_order_value
FROM orders
WHERE order_date >= '2025-01-01' AND order_date < '2026-01-01'
GROUP BY DATE_TRUNC('month', order_date)
ORDER BY month;`
- **rows**: gen=12 ref=12
- **application tool / answer / finish**: 1 / 1 / stop

### multi_05 — multi_step
- **Q**: 哪个销售区域的退货订单率最高？
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `WITH region_stats AS (
  SELECT r.region_name,
         COUNT(*) AS total_orders,
         COUNT(*) FILTER (WHERE o.status = 'returned') AS returned_orders
  FROM orders o
  JOIN regions r ON o.region_id = r.region_id
  GROUP BY r.region_name
)
SELECT region_name,
       ROUND(returned_orders::numeric / total_orders * 100, 2) AS return_rate_pct
FROM region_stats
ORDER BY return_rate_pct DESC
LIMIT 1;`
- **rows**: gen=1 ref=1
- **application tool / answer / finish**: 1 / 1 / stop

### multi_10 — multi_step
- **Q**: What is the average number of units per completed order?
- **Failure category**: RESULT_VALUE_OR_SHAPE_MISMATCH
- **Generated SQL**: `SELECT
  ROUND(SUM(oi.quantity)::numeric / COUNT(DISTINCT o.order_id), 2) AS avg_units_per_order
FROM orders o
JOIN order_items oi ON oi.order_id = o.order_id
WHERE o.status = 'completed';`
- **rows**: gen=1 ref=1
- **application tool / answer / finish**: 1 / 1 / stop
