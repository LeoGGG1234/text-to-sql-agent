# Text-to-SQL Agent Eval Report

**Provider**: deepseek　**Model**: default　**Date**: 2026-06-15T17:52:04.369Z

## Overall

| Metric | Value |
|--------|-------|
| Tests | 10 |
| Validity rate | 100.0% |
| Execution accuracy | 60.0% |
| Schema adherence | 100.0% |

## By category

| Category | Tests | Exec accuracy |
|----------|-------|---------------|
| simple | 4 | 75% |
| aggregation | 4 | 25% |
| join | 2 | 100% |

## Failed / inexact cases

### simple_04 — simple
- **Q**: 有多少订单被退货了？
- **Generated SQL**: `SELECT status, COUNT(*) AS order_count FROM orders GROUP BY status;`
- **rows**: gen=3 ref=1

### agg_02 — aggregation
- **Q**: 平均订单金额是多少？
- **Generated SQL**: `SELECT status, AVG(total_amount) AS avg_order_amount, COUNT(*) AS order_count
FROM orders
GROUP BY status
ORDER BY avg_order_amount DESC`
- **rows**: gen=3 ref=1

### agg_03 — aggregation
- **Q**: What is the highest single order total?
- **Generated SQL**: `SELECT order_id, total_amount, order_date, status
FROM orders
ORDER BY total_amount DESC
LIMIT 5;`
- **rows**: gen=5 ref=1

### agg_04 — aggregation
- **Q**: 每个忠诚度等级的客户平均下了多少订单？
- **Generated SQL**: `SELECT 
  c.loyalty_tier,
  COUNT(DISTINCT o.order_id) AS total_orders,
  COUNT(DISTINCT c.customer_id) AS total_customers,
  ROUND(COUNT(DISTINCT o.order_id)::numeric / COUNT(DISTINCT c.customer_id), 2) AS avg_orders_per_customer
FROM customers c
LEFT JOIN orders o ON c.customer_id = o.customer_id
GROUP BY c.loyalty_tier
ORDER BY avg_orders_per_customer DESC;`
- **rows**: gen=4 ref=4
