# Text-to-SQL Agent Eval Report

**Provider**: deepseek　**Model**: default　**Prompt**: v4　**Date**: 2026-08-15T11:40:50.856Z

## Overall

| Metric | Value |
|--------|-------|
| Tests | 20 |
| Validity rate | 100.0% |
| Execution accuracy | 60.0% |
| Schema adherence | 100.0% |

## By category

| Category | Tests | Exec accuracy |
|----------|-------|---------------|
| simple | 4 | 100% |
| aggregation | 4 | 75% |
| join | 4 | 75% |
| time_series | 4 | 25% |
| multi_step | 4 | 25% |

## Failed / inexact cases

### agg_03 — aggregation
- **Q**: What is the highest single order total?
- **Generated SQL**: `SELECT order_id, total_amount
FROM orders
ORDER BY total_amount DESC
LIMIT 1;`
- **rows**: gen=1 ref=1

### join_04 — join
- **Q**: 下单金额最高的 10 位客户是谁？
- **Generated SQL**: `SELECT c.customer_id, c.first_name, c.last_name, SUM(o.total_amount) AS total_order_amount
FROM customers c
JOIN orders o ON o.customer_id = c.customer_id
GROUP BY c.customer_id, c.first_name, c.last_name
ORDER BY total_order_amount DESC
LIMIT 10;`
- **rows**: gen=10 ref=10

### time_01 — time_series
- **Q**: Show the monthly sales trend for 2025.
- **Generated SQL**: `SELECT TO_CHAR(DATE_TRUNC('month', order_date), 'YYYY-MM') AS month, SUM(total_amount) AS total_sales
FROM orders
WHERE order_date >= '2025-01-01' AND order_date < '2026-01-01'
GROUP BY month
ORDER BY month;`
- **rows**: gen=12 ref=12

### time_02 — time_series
- **Q**: 2026 年每个季度的订单数量是多少？
- **Generated SQL**: `SELECT EXTRACT(QUARTER FROM order_date) AS quarter, COUNT(*) AS order_count
FROM orders
WHERE order_date >= '2026-01-01' AND order_date < '2027-01-01'
GROUP BY EXTRACT(QUARTER FROM order_date)
ORDER BY quarter;`
- **rows**: gen=2 ref=2

### time_03 — time_series
- **Q**: How many new customers signed up each month in 2025?
- **Generated SQL**: `SELECT TO_CHAR(signup_date, 'YYYY-MM') AS signup_month, COUNT(*) AS new_customers
FROM customers
WHERE signup_date >= '2025-01-01' AND signup_date < '2026-01-01'
GROUP BY signup_month
ORDER BY signup_month;`
- **rows**: gen=12 ref=12

### multi_02 — multi_step
- **Q**: 哪个品牌的利润率最高？利润 = 售价 - 成本。
- **Generated SQL**: `SELECT p.brand,
       SUM(oi.line_total) AS revenue,
       SUM(oi.line_total - p.cost * oi.quantity) AS profit,
       (SUM(oi.line_total - p.cost * oi.quantity) / SUM(oi.line_total)) * 100 AS profit_margin_pct
FROM order_items oi
JOIN products p ON oi.product_id = p.product_id
GROUP BY p.brand
ORDER BY profit_margin_pct DESC
LIMIT 1;`
- **rows**: gen=1 ref=1

### multi_03 — multi_step
- **Q**: What is the average number of items per order?
- **Generated SQL**: `SELECT COUNT(*) AS total_orders,
       (SELECT COUNT(*) FROM order_items) AS total_line_items,
       (SELECT SUM(quantity) FROM order_items) AS total_units,
       ROUND(CAST((SELECT SUM(quantity) FROM order_items) AS numeric) / COUNT(*), 2) AS avg_units_per_order
FROM orders;`
- **rows**: gen=1 ref=1

### multi_04 — multi_step
- **Q**: Platinum 等级客户贡献了多少比例的总销售额？
- **Generated SQL**: `SELECT
  SUM(CASE WHEN c.loyalty_tier = 'Platinum' THEN o.total_amount ELSE 0 END) AS platinum_sales,
  SUM(o.total_amount) AS total_sales,
  ROUND(100.0 * SUM(CASE WHEN c.loyalty_tier = 'Platinum' THEN o.total_amount ELSE 0 END) / NULLIF(SUM(o.total_amount), 0), 2) AS platinum_share_pct
FROM orders o
JOIN customers c ON o.customer_id = c.customer_id;`
- **rows**: gen=1 ref=1
